# Base URL for the backend API. In dev, Vite proxies /api to the backend (see vite.config.js),
# so this is mainly useful for production builds pointing at a deployed backend.
VITE_API_BASE_URL=http://localhost:8000
